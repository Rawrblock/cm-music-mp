// import { getToken } from "./utils/auth";

// app.ts
App({
    globalData: {},
    onShow() {
        // 不存在token || 判断当前是否是登录页面就不需要跳转
        // if (!getToken()) {
        //     wx.navigateTo({
        //         url: 'pages/login/index'
        //     })
        // }
    }
})